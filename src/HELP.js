export default `Usage: mdx-show [options]

start mdx-show server.

Options:
  -m, --mdx   The folder of md/mdx files which MUST md/mdx files be there. default: "./".
  -w, --web   The folder of web which MUST index.html be there. default: "{mdx-show}/web". Usually don't need to set.
  -p, --port  The port of web. default: "3000".
  -v, --version   Display the version number.
  -h, --help  Display help for command.`;
