const defaultVars = {
  name: 'mdx-show',
  staticServer: {
    port: 3000,
    root: './web',
  },
  storage: {
    root: '.',
  },
  logger: {
    fileSuffix: '-debug.log',
  },
};

export default defaultVars;
